# Opcional.
# Deixe vazio/indefinido em producao para usar o mesmo host da pagina.
# Use em desenvolvimento se o Vite estiver em uma porta e o servidor em outra.
# VITE_SOCKET_URL=http://localhost:3000
